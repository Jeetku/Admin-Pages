import AdminSidebar from "../../components/AdminSidebar";

const TransactionManagement = () => {
  return (
    <div className="adminContainer">
      {/* SideBar */}
      <AdminSidebar />
      {/* Main */}
      <main className="product-management">test</main>
    </div>
  );
};

export default TransactionManagement;
